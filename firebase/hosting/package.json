{
  "name": "lootbox-hosting",
  "version": "0.1.0",
  "scripts": {
    "emulator": "firebase emulators:start --only hosting",
    "deploy:staging": "firebase deploy --only hosting:staging  --project lootbox-fund-staging",
    "deploy:prod": "firebase deploy --only hosting:prod --project lootbox-fund-prod"
  },
  "engines": {
    "node": "16"
  }
}
